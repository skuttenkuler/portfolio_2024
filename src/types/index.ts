type skills = string[]

interface experience {
    title: string
    position: string
    deployed: string
    thumbnail: string
    description: string
    daterange:string
}
interface projects {
    title: string
    deployed: string
    code: string
    thumbnail: string
    tech: string[]
    description: string
}
